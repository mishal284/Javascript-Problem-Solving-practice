let isSnow = true;
let isRain = true;
let isSpeed = 15;
if (isRain && isSnow && isSpeed) {
    alert('The rocket have been launched');
    
}
else{
    alert('The rocket have not been launched');
}