let games = ['Basketball','Baseball','Cricket','Throwball','Tennis','Hockey'];
  // add polo as a member at the end of array
  games.push('polo');
  console.log(games)
  //add polo as a member at the begining of the array
  games.unshift('polo');
  console.log(games);
  // remove the member of aay from the end
  games.pop();
  console.log(games);
  // remove the member of array from the beginning
  games.shift('polo');
  console.log(games);

